   pool-info
       Syntax:

          pool-info [--bytes] pool-or-uuid

       Returns  basic information about the pool object. If --bytes is specified the sizes of basic info are not con-
       verted to human friendly units.

