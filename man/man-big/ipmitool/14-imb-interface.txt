IMB INTERFACE
       The ipmitool imb interface supports the Intel IMB (Intel Inter-module Bus) Interface through the /dev/imb  de-
       vice.

       You can tell ipmitool to use the IMB interface with the -I option:

       ipmitool -I imb <command>

